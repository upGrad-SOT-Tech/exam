export const IPC = {
  RUN_ALL: "system-checks:run-all",
  RUN_MEDIA: "system-checks:run-media",
  GET_DEFINITIONS: "system-checks:get-definitions",
  CLEAR_CLIPBOARD: "system-checks:clear-clipboard",
} as const;
